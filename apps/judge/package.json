{
  "name": "@litecode/judge-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Go code-execution engine. Turborepo package boundary only — the real toolchain is Go.",
  "scripts": {
    "build": "go build -o dist/judge ./cmd/judge",
    "test": "go test ./...",
    "lint": "go vet ./...",
    "dev": "go run ./cmd/judge"
  }
}
